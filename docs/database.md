# Database Design

Database: PostgreSQL.

All foreign keys must be indexed.

Large tables must support pagination.

Activity logs must support partitioning.

Use composite indexes for frequent filters.

Avoid loading unnecessary relationships.
